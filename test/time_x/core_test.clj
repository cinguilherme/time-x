(ns time-x.core-test
  (:require [clojure.test :refer :all]
            [time-x.core :refer :all]
            [time-x.util :refer :all]))

(deftest now-test
  (is (not-nil?)))

(deftest after?-test
  (is (= 1 1)))

(deftest very-close?-test
  (is (= 1 1)))
